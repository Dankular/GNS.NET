# GNS.NET open work

Open work is consolidated into [`PROJECT_STATUS.md`](PROJECT_STATUS.md), the single authoritative TODO and gap register. It distinguishes incomplete implementation (`[ ]`), supported partial work (`[~]`), and interface-only capability. Update the canonical file only after implementation, integration, tests, and verification.
